# Digit Draw App

Project scaffold for a handwritten digit drawing and recognition web app.

## Structure

- app/: Flask app source code
- saved_model/: trained PyTorch model checkpoint
- requirements.txt: Python dependencies

## Run

1. Install dependencies:
   pip install -r requirements.txt
2. Start app:
   python app/main.py
